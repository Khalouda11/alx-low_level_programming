#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: a C program which uses the puts function to print
 *              a message to the standard output
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
